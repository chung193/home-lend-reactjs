/*
 * Blank Page Messages
 *
 * This contains all the text for the Maintenance.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.Pages.Service.paper.tab.SettingAndFirmware.tab';

export default defineMessages({
  settingTab: {
    id: `${scope}.setting`,
    defaultMessage: 'Setting',
  },

  firmwareTab: {
    id: `${scope}.firmware`,
    defaultMessage: 'Firmware',
  },
});
